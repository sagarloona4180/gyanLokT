import { MakeChildDirective } from './make-child.directive';

describe('MakeChildDirective', () => {
  it('should create an instance', () => {
    const directive = new MakeChildDirective();
    expect(directive).toBeTruthy();
  });
});
